<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>提起の作成</title>
    <link rel="stylesheet" href="/header.css">
    <link rel="stylesheet" href="/style.css">
    <script type="module" src="/ui.js"></script>
  </head>

  <body>
  <header>
    <h1><a href="/">UTonomy</a></h1>
    <button id="drawer-toggle">☰</button>
    <div id="drawer" class="drawer">
      <div class="account">
        <% if (user) { %>
            <a href="/user"><img src="<%= user.photos[0].value %>"><%= user.displayName %></a>
            <a href="/logout">ログアウト</a>
        <% } else { %>
            <a href="/auth/google">ログイン</a>
        <% } %>
      </div>
      <nav>
        <ul>
            <li><a href="/">提起ひろば</a></li>
            <% if (user) { %>
              <li><a href="/group">グループ</a></li>
            <% } %>
        </ul>
      </nav>
    </div>
  </header>
  <main>
    <h1>
      「<%= post.title %>」にコメント
    </h1>
    <form id="post" action="/comment/<%= post.id %>" method="post">
      <% if (user) { %>
          <input name="nickname" value="<%= user.displayName %>" placeholder="自称理事">
          <input name="title" placeholder="こういう問題があるのでは？" required>
          <button type="submit">送信</button>
      <% } else{ %>
        <p>
          コメントするには<a href="/auth/google">ログイン</a>
        </p>
      <% } %>
    </form>
  </main>
  </body>
</html>